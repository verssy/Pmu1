package ru.drus.lab1fixed;

public class MyClass {
    static public int myMax(int x, int y) {
        if (x > y)
            return x;
        return y;
    }
    static public int myMin(int x, int y) {
        if (x < y)
            return x;
        return y;
    }
}
